For FINAL deployment:

1) Re-enable the ROOM ID system 										(for unique room ids, instead of always "SHIP")

2) Uglify/minify JS again 												(by enabling the WebPack plugin in webpack.config)